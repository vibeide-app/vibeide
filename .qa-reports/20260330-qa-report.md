# QA Report (Read-Only)

```
QA REPORT (READ-ONLY)
════════════════════════════════════════
URL:         localhost:5173 (Electron dev)
Branch:      vibeide/claude-99198f97
Tested:      2026-03-30
Commit:      b4b3aef (+ uncommitted fixes)

FINDINGS BY SEVERITY
────────────────────────────────────────

CRITICAL (2)

  [1] Sidebar inline width overrides CSS collapsed width
      Page: Main workspace
      Expected: Collapsed sidebar is 48px via CSS .agent-sidebar.collapsed
      Actual: Saved inline style.width (e.g. "240px") from settings/resize overrides
              the CSS rule, so sidebar stays at saved width when collapsed.
              The resize handle also sets inline width during drag, which persists
              and fights with the collapse CSS.
      Likely file: src/renderer/src/main.ts:103 (settings restore)
                   src/renderer/src/main.ts:129 (resize drag)
      Fix: Clear inline style.width when collapsing. Set it back when expanding.
           In toggleCollapse/setCollapsed: if collapsed, remove sidebarEl.style.width;
           if expanded, restore saved width.

  [2] Sidebar expanded content has min-width: 240px causing horizontal overflow
      Page: Main workspace (collapsed state)
      Expected: Collapsed sidebar is 48px with no scrollbar
      Actual: .sidebar-expanded-content has min-width: 240px in CSS. Even though
              display:none hides it, if there's any timing issue or if the
              browser calculates layout before display:none applies, the sidebar
              container would have a 240px minimum. Also, the expanded content
              div is never removed, only hidden — if display:none fails to apply
              (e.g., specificity issue), the 240px min-width would force the
              sidebar wider than 48px.
      Likely file: src/renderer/src/styles/terminal.css:184
      Fix: Remove min-width from .sidebar-expanded-content. It's unnecessary
           since the parent .agent-sidebar already has width: 240px when expanded.

HIGH (3)

  [3] Command palette filtered results lose relevance ordering
      Page: Command palette (Ctrl+Shift+P, then type a query)
      Expected: Filtered results sorted by fuzzy match score (best match first)
      Actual: Results are re-grouped by category after sorting, so within each
              category group the order is preserved, but the best overall match
              may be buried under a less-relevant category header. E.g., typing
              "th" shows the Theme category header first (if any theme commands
              match) even if a non-theme command like "Toggle Theme Switcher"
              in the View category is a better match.
      Likely file: src/renderer/src/ui/command-palette.ts:241-258
      Fix: For filtered results, show flat scored list (original behavior)
           or add per-row category badges instead of full group headers.
           The grouping should only apply to the unfiltered (empty query) view.

  [4] Sidebar resize handle still works when collapsed to icon rail
      Page: Main workspace (sidebar collapsed)
      Expected: Resize handle should be hidden or disabled when sidebar is collapsed
      Actual: The .sidebar-resize-handle is always visible and draggable. Dragging
              it while collapsed sets an inline width that breaks the 48px rail
              layout and creates an inconsistent state.
      Likely file: src/renderer/src/styles/terminal.css (sidebar-resize-handle)
                   src/renderer/src/main.ts:120-149 (resize handler)
      Fix: Hide resize handle when collapsed:
           .agent-sidebar.collapsed .sidebar-resize-handle { display: none; }
           Or disable the mousedown handler when collapsed.

  [5] Terminal loading overlay may stack on reconnect/reattach
      Page: Terminal pane
      Expected: One loading overlay per terminal, dismissed on first output
      Actual: setLoadingInfo() creates a new overlay unconditionally. If
              reattach() is called (e.g., layout rearrangement), createTerminal()
              returns the existing panel and calls setLoadingInfo() again,
              creating a second overlay on top of the first. The
              receivedFirstOutput flag prevents this only if output already
              arrived, but during a layout move before first output, it would
              stack.
      Likely file: src/renderer/src/terminal/terminal-panel.ts:66-68
      Fix: Guard setLoadingInfo: if this.loadingOverlay already exists, skip.

MEDIUM (4)

  [6] Rail doesn't update when agents start/stop/change status
      Page: Icon rail (collapsed sidebar)
      Expected: Agent icons in the rail update when agent status changes
      Actual: renderRailContent() is only called on collapse, active project
              change, and project list updates. updateAgentStatus() and
              updateAgents() don't re-render the rail, so a new agent spawned
              while the sidebar is collapsed won't appear in the rail until
              you toggle.
      Likely file: src/renderer/src/project/project-sidebar.ts:253 (updateAgents)
                   src/renderer/src/project/project-sidebar.ts:260 (updateAgentStatus)
      Fix: Add `if (this.collapsed) this.renderRailContent();` to the end of
           updateAgents() and updateAgentStatus().

  [7] Empty state "Open Folder" button has duplicate icon append
      Page: Sidebar (zero projects)
      Expected: Dragonfly icon appears once
      Actual: The code appends the icon div twice:
              Line ~628: empty.appendChild(icon);  // first append
              Line ~633: empty.appendChild(icon);  // second append (same element)
              The second appendChild() actually moves the element (DOM spec),
              so it's not visually duplicated, but the first msg element
              ends up as the first child instead of the icon.
      Likely file: src/renderer/src/project/project-sidebar.ts:~625-640
      Fix: Remove the first empty.appendChild(icon) call.

  [8] Space Grotesk may not render in onboarding wizard
      Page: Onboarding wizard
      Expected: Onboarding text uses Space Grotesk
      Actual: The onboarding headline uses hardcoded font-size: 24px but
              inherits font-family from body (Space Grotesk). However, the
              @font-face for Regular (400) uses woff2 format while Medium
              and Bold use TTF. If the browser/Electron build has different
              caching behavior for woff2 vs TTF, there could be a FOUT
              (flash of unstyled text) during first load.
      Likely file: src/renderer/src/styles/global.css:1-6
      Note: Low probability, Electron's Chromium handles woff2 well.

  [9] aria-live announcements not throttled for rapid status changes
      Page: Main workspace with multiple agents
      Expected: Screen reader announces status changes clearly
      Actual: If 4 agents change status within 1 second, 4 rapid
              announcements stack. Screen readers may drop or garble them.
      Likely file: src/renderer/src/main.ts:~195 (announceStatus)
      Fix: Debounce announcements to max 1 per 2 seconds, queue latest.

LOW (2)

  [10] Toast background tint very subtle at 6% opacity
       Page: Toast notifications
       Expected: Event type is visually distinguishable
       Actual: 6% opacity tint is barely visible on dark themes. The colored
               dot (8px) is the primary indicator, but the old border-left was
               more visible. May need to increase to 8-10% for dark themes.
       Likely file: src/renderer/src/styles/global.css:557-559

  [11] Rail logo button size (32px) doesn't match project buttons (36px)
       Page: Icon rail
       Expected: Consistent button sizing in the rail
       Actual: .rail-logo-btn is 32x32px but .rail-project-btn is 36x36px.
               The dragonfly icon renders at 28px inside 32px button, while
               the letter avatar renders at 28px inside 36px button. The
               logo button looks slightly smaller/off-center.
       Likely file: src/renderer/src/styles/terminal.css:199-201 vs 224-226

HEALTH SCORE: 5/10

RECOMMENDED FIX ORDER:
  1. [Critical #1] Inline width override — causes the most visible layout bug
  2. [Critical #2] Remove min-width: 240px — related to #1, prevents edge cases
  3. [High #3] Revert filtered palette to flat list — breaks usability of search
  4. [High #4] Hide resize handle when collapsed — prevents #1 from recurring
  5. [Medium #6] Rail agent status updates — icon rail goes stale
  6. [Medium #7] Duplicate icon append — cosmetic DOM ordering issue
  7. [High #5] Loading overlay guard — prevents stacking on reattach
  8. [Medium #9] Aria-live throttle — accessibility improvement
  9. [Low #10-11] Visual polish

════════════════════════════════════════
```
